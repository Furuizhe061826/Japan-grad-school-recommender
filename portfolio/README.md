# 作品集导出说明

文件：`portfolio.html`

打开方式：

1. 在文件管理器中进入 `portfolio` 文件夹。
2. 双击打开 `portfolio.html`，或用 Chrome / Edge 打开。
3. 浏览器中按 `Ctrl + P`。
4. 目标打印机选择「另存为 PDF」。
5. 纸张选择 A4，边距选择「无」或「默认」，勾选「背景图形」。
6. 保存为 PDF 后即可上传网申系统。

如果后续要替换截图，可以把首页、输入页、结果页截图放到第 3 页对应占位区。
